# Changelog · 独立端

`@bilibili-notify/server` + `@bilibili-notify/web` 独立端版本历史。Docker 镜像
`akokk0/bilibili-notify` / `ghcr.io/akokk0/bilibili-notify` 跟随 `v<VERSION>`
git tag;发布 workflow 会在构建前按 tag 临时同步 apps 版本元数据。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/);本仓库 koishi
端 `koishi-plugin-bilibili-notify*` npm 包版本独立维护,见各包 `CHANGELOG.md`。

---

## [0.10.1] — 2026-09-07

修了三处:链接解析的 QQ 小程序卡点开「页面不存在」、通知卡的「去更新」跳过去停在半路、空闲关浏览器偶尔撞上正在开始的渲染让那张卡出不来。正式版,Docker 的 `:latest` 从 0.10.0 挪到这里。

### Fixed

- **链接解析的 QQ 小程序卡点开是空白或「页面不存在」**:这个功能从 0.10.0 发出那天起就是坏的 —— 卡能发出来、封面标题都对,点进去小程序却找不到页面。原因是向腾讯签卡时把视频网址填进了「小程序页面路径」那一格,网页链接那一格反倒空着,签出来的卡里连网页链接都没有。现在按 B 站 App 自己分享出来的卡的形状签:路径是小程序的视频页,网页链接另给。真机验过,手机 QQ 点开直接进视频 (49d8505d)
- **「去更新」跳到系统页后停在半路**:从概览卡或右下角通知卡点「去更新」,页面滚到「系统」页却没停在「更新」那一节 —— 标题被顶栏遮住一截,上面几节的内容加载完再长高时还会把它推走。现在会停在顶栏正下方,并在上面几节加载完之前跟着重新对位;你一滚动它就不再动 (66011c0d, b5ee04fb)
- **空闲自动关浏览器偶尔让一张卡渲染失败**:浏览器空闲一段时间后自动关闭的那一下,可能正好撞上一次渲染的开头,浏览器被从渲染底下关掉,那张卡就失败(推送历史里记一次失败,或链接解析没出卡)。现在关之前先等正在开始的渲染让路 (43a82959)

---

## [0.10.0] — 2026-09-05

推送历史改成「一次推送一行」:一次推送的多条消息收在同一行、可以展开逐条看,没有可用目标的推送也会留一张卡;链接解析新增 QQ 小程序卡。

⚠️ **弹幕词云与 AI 总结改成了「下播」的附加项,不再各自独立开关。** 升级时按老配置自动合并:三者任一开着,「下播」就是开的;下播的推送目标取三者并集;两个附加项保持原来的开关状态。**此前只开了词云 / 总结、没开下播的主人,升上来会开始收到下播卡片本身** —— 不想要就把「下播」整项关掉(附加项跟着停),详见 Changed。正式版,Docker 的 `:latest` 从 0.9.1 挪到这里。

### Added

- **没有推送目标的推送也会在面板上留一条**:此前订阅开着、但这类推送没配目标(或配的目标全停用)时,推送静悄悄地什么都不做,面板上一点痕迹都没有。现在照样在「推送历史」里记一行、右下角弹一张警示色小卡标「无目标」,卡上的「去配置」直接跳到那位 UP 的抽屉并停在推送目标那一节。这一行不进「今日推送」与「今日失败」——它确实没推到任何地方 (af6d032c, ca8ac15e, 0f5c7b35)
- **链接解析可以回 QQ 小程序卡**:「系统」页链接解析一节的每一行多了「形式」—— 图片卡(默认)或小程序卡。小程序卡就是 B 站 App「分享到 QQ」那种,点开直接进小程序播放。它要机器人所在的 OneBot 实现提供 `get_mini_app_ark` 接口(目前已知只有 NapCat 有),独立端自己探测:同一节里新增「适配器支持情况」面板,逐个 OneBot 适配器显示支持 / 不支持 / 未探测。签不出卡或发送失败时自动回落图片卡,群里不会空着;QQ 官方机器人做不到,一律图片卡 (6dcf4bd4, 5b6727fc, 53129268, 869a4964)
- **「有新版」的通知卡说清是哪一版、带来什么**:卡上写版本号、那一版 CHANGELOG 的概述,以及当前状态。下载改成后台跑 —— 点了之后卡片立刻变「下载中」,面板照常用,下载完同一张卡换成「已就绪,可以重启应用」或「下载失败」。GitHub Release 的正文也会带上该版本的 CHANGELOG 整段 (c62ff533, 27d9db85, da3b558e, 11cb2bbd, debe08ed, 959617a1, e588a066)

### Changed

- **⚠️ 弹幕词云与 AI 总结改成「下播」的附加项**:此前它们是两个独立特性,各有开关与推送目标;现在挂在「下播」下面当两个附加项,共用下播的开关与目标(升级迁移见上方 ⚠️ 段)。一次下播从此是**同一次推送**:下播卡先发,词云与 AI 总结算好之后追加到同一行 —— 右下角那张小卡会从「1 条」长到「3 条」,历史里也是一行 (999906c2, aa16cfc4)
- **推送历史一行 = 一次推送 × 一个目标**:一次推送里的多条消息(卡片本体、@全体、图集、词云、AI 总结)收在同一行,列表上显示首条文案加一颗「N 条」胶囊,点开逐条看文案、图和送达结果。状态从「成功 / 失败」两态改成四态:已送达、部分失败(本体到了、附加项没到)、失败(本体没到)、无目标。老的历史记录照常显示,不用迁移 (af6d032c, bb17ae09, ed7c5c34, 1a4ae98d, 1e9ad594)
- **链接解析的「生效范围」换成一张表**:0.9.1 的「所有群 / 仅以下群 + 勾选白名单」改成「默认行 + 按群例外」,两列分别是解析与形式,每格三态(跟默认 / 显式开 / 显式关),像每位 UP 的覆盖那样。「所有群」不再是一种模式,它就是默认行;想只在某几群解析 = 默认行关掉 + 那几群显式开。老配置读时自动迁移,不用动手;按群例外默认收起,群多时不会把卡撑爆。**有一处行为变化**:默认行开着时,停用的目标群也不解析了(0.9.1 的「所有群」不看停用) (ac708b74, 149e3f2a, 53668899)
- **停用的推送目标不再当推送候选**:此前推给一个停用的目标(目标本身停用,或它挂的适配器停用)会一路退避重试约三分钟才记成失败;现在直接不当候选,该类推送的目标全停用时视同「无目标」 (af6d032c)
- **「今日推送」数的是推到了多少个地方**:一次动态推给三个群算 3 次,而不是 1 次;「今日失败」数的是失败与部分失败的行数,趋势图同口径 (bb17ae09, 1e9ad594)
- **周期「正在直播」在历史里单列一类**:此前它和开播都记成「开播」,分不出哪条是复推;开关与推送目标仍与开播共用 (1a4ae98d)
- **图集发送失败不再静默**:一条图文动态的图集单独发失败时,那一行记「部分失败」、在历史里看得到;仍然不重发(重发会把主卡也带出来) (2423cd66)

### Fixed

- **QQ 官方机器人的推送目标会白发一条 @全体、并把整次推送标成失败**:官方机器人根本发不了 @全体,而只要订阅的 @全体 开关开着,推送时仍会先给它发一条独立的 @全体 消息 —— 那条必然失败。UP 抽屉里那个开关对官机目标本来就显示为关,发送时的行为却对不上。现在不支持 @全体 的平台一律跳过那条消息 (3ddf55bc)
- **周报「发送到」里,适配器被停用的目标显示为启用**:0.9.1 起选择器会给停用的目标标「已停用」,但它只看目标自己的开关 —— 目标开着、它挂的适配器被停用时不标,而发送时本来就会跳过它。现在面板上的判定与发送时的判定是同一条 (788cec1c)

---

## [0.9.1] — 2026-09-04

链接解析学会了认群:可以只在你点名的群里出卡,其余群贴链接不理;周报的推送目标与它用同一套「停用」规矩。**有一条要动手的**:自定义文案里还留着 `{url}` / `{link}` / `-link` 的,这一版起会原样打出来,请到「高级规则」页把它删掉(详见 Changed)。正式版,Docker 的 `:latest` 从 0.9.0 挪到这里;留在正式渠道的实例在「系统」页检查更新即可收到。

### Added

- **链接解析生效范围**:「系统」页「链接解析」一节新增「生效范围」—— 「所有群」照旧,「仅以下群」则从推送目标里勾选(只列群类目标,OneBot 与 QQ 官方机器人两种都行),没勾的群贴链接不理,勾选立刻生效、不用重启。被停用的目标(目标本身停用,或它挂的那个适配器停用 / 已删)即使勾着也不解析,芯片上标「已停用」提醒你;推送目标里一个群都没有时给出空态并指向推送目标页。升级上来的老配置按「所有群」走,行为不变 (4b1d36b1, 9b0eaa7c, 70141f77, 2344e8e8, 90346132)

### Changed

- **⚠️ 文案模板不再替你抹掉 `{url}` / `{link}` / `-link`**。自 0.5 起链接就是消息版式里独立的「链接」块,文案模板没有链接变量;此前为了兼容更早的自定义文案,渲染时会悄悄把这三个老占位符删掉,这一版起不删了 —— 模板里还留着它们的,推送出来会原样显示 `{url}`。请到「高级规则」页的「动态文案 / 视频文案 / 开播 / 直播中 / 下播」模板(以及每位 UP 的覆盖)里把它删掉;模板还是 0.5 之前那份默认文案的,点「默认文案有更新」一键换新即可。想显示链接,在版式编辑器里开「链接」块 (7a62f023, b6dfbb38)
- **周报「发送到」对停用目标的处理**:选择器改成与链接解析同款的芯片,停用的目标照列、标「已停用」、勾选状态保留;发送时跳过它们并在结果里写「跳过 N 个已停用」,不再算作发送失败;勾的目标全都停用时视同没配目标,不生成周报,通知主人「目标都已停用」 (1631811e, ee3251ee, 5ee85f3e, 49d1b9ea)
- **桌面安装包与 Docker 镜像、应用内更新装的是同一份东西**:安装包里不再带整棵 node_modules,只有打包好的服务端、界面和一个 Node,体积随之缩小;行为不变 (b16033ba)
- 配置文件里若残留 Koishi / AstrBot 端专属平台(`koishi-bot` / `astrbot`)的适配器或推送目标,启动时静默丢弃 —— 独立端从没提供过这两种,只有从那两端搬过数据来的人会遇到 (2261cd59, 7bd2305f)
- 从源码跑的开发版(版本号 `0.0.0-dev`)不再检查更新:此前它比任何发布过的版本都「旧」,每次开面板都提示有新版;现在「系统」页写「开发版,不检查更新」。Docker 与桌面版用户不受影响 (4148d420)

### Fixed

- **应用内更新按了「立即重启并应用」后没有任何反应**:更新其实成功了,只是界面上什么都不显示 —— 没有重启中状态、不刷新、也不告诉你换到了哪一版。现在会等新进程真正起来后整页刷新,并弹「已更新到 X」/「已退回 X」;90 秒没等到会提示多半是容器没配重启策略,可以再等;新进程报的版本对不上(新版没起来、启动器退回了)也会明说。服务卡住时按钮不再无限转圈 (1aee5390, 366e41e8, 16b34654)

---

## [0.9.0] — 2026-09-03

从 alpha 系列毕业的正式版 —— Docker 的 `:latest` 从 0.8.0 挪到这里。相对上一个 alpha 没有新的面向用户的改动,只是收尾。0.8.0 之后的两件大事都在下面 alpha.1 / alpha.2 两段里:**应用内更新**(以后换版本在「系统」页点一下,不必重拉镜像、重下安装包)和**链接解析**(群里贴 B 站视频链接自动回一张视频卡,默认关)。

⚠️ **从 0.8.0 直接升上来的主人请先看这段。**

- **这一次仍得拉镜像 / 装安装包** —— 应用内更新是这一版才带上的,它是第一个能被后续版本在应用内更新到的正式版。从这一版起,留在「正式版」渠道检查更新就能收到之后的正式版;alpha.1 里说的「正式渠道还没有清单」到此结束
- **镜像默认 CMD 改经启动器 `boot.mjs`**,自己覆盖过 CMD 的请对一下(详见 `0.9.0-alpha.1` 的 Changed)
- **镜像不再注入 `BN_WEB_DIST`**,首次启动种进 `bn.config.yaml` 的那行 `webDistDir: /app/web-dist` 会在下次启动时自动删掉;自己改过那个值的请看 `0.9.0-alpha.1` 的 Changed
- 已经跑着 alpha.1 / alpha.2 的:预发布渠道同样会收到这一版,在应用内更新即可,之后想回正式渠道再切

---

## [0.9.0-alpha.2] — 2026-09-03

链接解析上线:群里有人贴 B 站视频链接,机器人自动回一张视频卡片。这也是**第一个能在应用内装上的版本** —— 已经跑着 0.9.0-alpha.1 的,把「系统」页的更新渠道切到「预发布」、检查更新即可;更早的版本仍得拉镜像 / 装安装包。

### Added

- **链接解析**:群里有人贴 `bilibili.com/video/BV…` / `av…` 链接或 `b23.tv` 短链,机器人自动回一张视频卡片 —— 用的就是推送动态卡那一套,你在版式编辑器里排的块顺序、装的皮肤都跟着走。**默认关**,「系统」页新增「链接解析」一节:总开关,加一个冷却时间(同一个群同一个视频多久内只出一次,默认 60 秒,0 = 不节流)。开着就意味着机器人在的**所有群、谁贴都算**,所以这个开关得是你自己按的;私聊里贴不理。失败一律沉默:视频不存在、没配 Chrome、发送失败,都只记日志、不在群里回话。另有两条不可调的底线防换着视频刷 —— 每个群每分钟最多 6 张、全局同时最多处理 3 张(超了直接放弃,不排队,免得把真正的开播 / 动态卡挤到后面)。OneBot 直接可用;QQ 官方机器人要群主在群设置里把机器人的消息范围放到「获取群内全部消息」,留在「仅 @ 机器人」那档的话得 @ 它再贴链接 (1c207f55, 054de844, e4258dd6, 8515a5ab, 0b704c65, fbbdecd6, 21a5d998, 26cb201a, 1e4e3807, 15933417, bf16508b)

### Fixed

- 推送目标页的 QQ 会话发现列表:一条没带名字的后续事件(比如机器人被拉进群)不再把先前记住的那个名字抹掉 —— 群事件本来就不带群名,那个名字是列表里唯一能认的东西 (83827240)

---

## [0.9.0-alpha.1] — 2026-09-02

应用内更新上线:Docker 与桌面版从此可以在「系统」页里直接换版本,不必再重拉镜像、重下安装包。**从 0.8.0 升到这一版仍得照老办法拉镜像 / 装安装包** —— 机制是这一版才带上的,它是第一个能被后续版本在应用内更新到的版本。这是 alpha:想在应用内收到后续 alpha,把「更新渠道」切到「预发布」;在下一个正式版发出之前正式渠道还没有清单,留在「正式版」渠道检查会报「连不上更新服务器」,那不是你的网络问题。

### Added

- **应用内更新**:「系统」页新增「应用内更新」一节 —— 检查更新、下载、「立即重启并应用」三步,新版本装进数据目录的 `versions/<版本>/` 下,镜像 / 安装包自带的那份永远不动。默认只自动下载(可关),**什么时候重启换版本永远由你按**;应用那一刻服务会重启几秒,推送断一下、直播监听重连。**容器部署请确认 `restart` 策略开着**(compose 模板的 `unless-stopped` 就够),否则进程退出后没人把它拉起来;桌面版由外壳自动拉起,界面跟着刷新。更新渠道分正式版 / 预发布,预发布默认关。同一份包这个进程装过了就不再下;盘上只留当前 + 新装的那一版,更老的自动清掉 —— 只碰长得像版本号的目录,你放进 `/data` 的自己的东西不动。清单和更新包都带签名:验不过的一律不装、盘上什么都不留;比之前见过的旧的清单也不收,代理站缓存或回放旧文件推不回去。新版本要求更高的运行环境(Node)时会明说「请重拉镜像」,不会装上起不来。自己构建的版本没有内置公钥,这一节会说明「本构建未启用」而不是报错 (b6e0e3ff, f3819654, 0ef41f12, cd6e07a5, 3dc9e2d4, fcbcae2f, a8456c94, 006ff174, aed3d192, af24d0c1, 56ff665e, d360a9f3, b3fd8ce0, e08c8f15, 231f4c77, f82f9fbf, a43493dc, 07389758, c5d5a728, 5b13e822, 495a9dc7, 0c0d3b93, 45f44246, 2dea2e64, 92dc633f, 85943621, b7e7feaa, 7ef62ce3, c73e7643, 611819fc, 1d95927c)
- **回退与自愈**:装了新版之后可以一键「退回上一版」(只退一步,不给版本列表)。退回后那一版会被钉住 —— 打开面板不再自动查更新,免得一开面板就把你按的回退撤销了;手动按「检查更新」才是明确要往前走,装上新版会拔掉钉子。新版本要是起不来,启动器连试几次后自动判死、退回上一个能起的版本,不用进面板。我们撤回一个坏版本时:没升的人不会装它,已经下好等重启的那份会被删掉,正在跑着它的人会被带到清单指定的那一版(哪怕版本号更小),重启后不再选它 (73dbbc2f, e75a5eeb, 82eac3af, d6aaaa4f, c6d5b5ef, 084fca87, 436fb88a, f23f4527)
- **有新版时的提示**:打开面板查一次(不定时、不轮询),查到新版右下角出一张带「去更新」按钮的通知卡、不自动消失;概览「系统状态」卡的版本号旁边也标一句,点过去直接滚到系统页那一节。没新版就什么都不说 (6db3d61d, bfd04382, a4e1b38a, 2ba17ead, 0550f67f)
- **下载加速站**:连不上 GitHub 时可以在更新那一节选一个代理站 —— 内置六个候选加一行自定义,「测一遍」由服务端逐个测延迟、并列出每站拿到的清单版本,签名验不过 / 缓存了旧清单 / 连不上分别标出来。默认直连、只能选一个,直连永远垫底兜着;更新包有签名,代理站改不了内容,最多让这次下载失败 (39518ddf, 5440396f)
- 更新那一节会显示新版本的发布说明,并附发布页链接 (78bdd94b)

### Changed

- **Docker 镜像不再注入 `BN_WEB_DIST`;首次启动种进 `bn.config.yaml` 的 `webDistDir: /app/web-dist` 那行会在下次启动时自动删掉**。那行从来不是你填的(界面上根本没有这个字段),留着它就把界面钉死在镜像自带的那份上 —— 应用内更新换了后端、界面却不跟着走。只删值恰好是 `/app/web-dist` 的那行,你自己的注释和排版不动;自己改成别的路径的会保留,但那样应用内更新就换不了界面。配置目录只读时只记一条日志、照常启动 (693da0cb, be4ab102)
- 镜像与桌面版都改经启动器 `boot.mjs` 起服务:它先在镜像 / 安装包自带那份与数据目录里更新过的版本之间选一个,再加载;版本号与界面资源都跟着实际加载的那份走。镜像默认 CMD 已随之改变,自己覆盖过 CMD 的请对一下 (3891c97f, cb0db077, 2d94dc3f)

---

## [0.8.0] — 2026-08-31

新手指引上线:第一次打开会有人带着你从登录一路走到收到第一条推送,不必再对着一堆设置页自己猜顺序。QQ 官方机器人也多了扫码接入这条路 —— 扫一下就能把自己的机器人接进来,或者当场建一个,不用再自己去开放平台折腾应用。

### Added

- **新手指引导览**:第一次打开会先问你是新手还是老用户 —— 说要指引,页面边上就常驻一个可收起的导览标签,按 **登录 → 适配器 → 推送目标 → 发一条测试 → 订阅 UP** 五步带着走;每一步用聚光灯把当前该点的地方照出来、其余变暗,点完自动进下一步,走完有个小庆祝。随时可以跳过,「系统」页的「新手指引」一节是重新打开它的地方。老用户选「不用了」就整个不再出现,不打扰 (a5f62c95, e9e8f54f, 5efd0880, 59ab1c8d, 03c3cb5b, 5e62f6c2, fcfa1152, 46da8260, 5fbda4be, efebd856, 8e829c25, 7fd02108)
- **教程**:「关于」页新增「新手指引」一节,把配置里最容易卡住的地方写成了几章可以慢慢读的文档 —— 三种适配器怎么选、QQ 那条路具体要办哪些手续、推送目标从哪来。导览是带着你做,教程是让你看明白为什么 (f6d8b126, f5801952, 66031080, 3ab88f34, f12fe78d, 05c4834b)
- **QQ 官方机器人扫码接入**:QQ 适配器表单里多了个扫码按钮 —— 用手机 QQ 扫一下,已有的机器人直接把 AppID / Secret 回填进表单,没有的话可以当场建一个,不用再自己去 QQ 开放平台申请。回填的是草稿,你看过再保存。**注意这条路建出来的是 lite 机器人**,能推的范围有限(自己的私聊 + 自己当群主的群),过审和企业认证会逐级放宽 —— 表单里写清楚了 (79e13f54, 4c0a35dc, dfe38145, c7589124, 71fec587, 7e6e9ff6, 2dc76c20)
- 概览页的数据卡片改成与统计页同一套样式,两页之间不再有观感落差 (c86b56e9)

### Changed

- **界面语汇统一**:同一个意思在不同页面长得不一样的地方收敛成一套 —— 「新建」类的虚线空位(新增订阅 / 新增技能 / 导入备份…)统一成一个家族、选中态统一成粉色实心块(每位 UP 的标签页此前是白底粉框的另一套,现在跟全局标签一致)、提示盒补齐第四档「说明」、散落的手写字号收进字号表。功能没变,只是不再东一套西一套 (c4b5ca1b, 7a2e38ee, cc643559, 4790bcf7, b707fcc8, c2752a21, 625cbf8f, 8b9d92e8, 49b57784, 18e59925, f4a7ed52, 6ff82e84, 90977688)

### Fixed

- **桌面版:强制退出或崩溃后,下次可能整个起不来**。关掉主程序并不会带走后台的服务进程 —— 它会被系统收养继续活着,占着数据目录。运气不好的话下次启动就直接失败,而且报错完全指不到这个方向(后台进程起来就被系统杀掉,连日志都留不下)。现在服务进程自己会盯着主程序,主程序没了就主动退出 (7d8f45e1)
- **订阅页的分组胶囊在壁纸皮肤下几乎看不见**:那一排胶囊原本用的是半透明底,只在默认白底页面上好看,换成带壁纸的皮肤后花花的背景直接透上来 (0f140a7c)
- **下拉类按钮不向读屏软件报告展开状态**:按钮组件把调用方写的 `aria-expanded` / `aria-haspopup` 覆盖掉了,读屏软件因此读不出「这个按钮能展开、现在是展开还是收起」 (bd7889e1)

---

## [0.7.1] — 2026-08-28

直播信息流客户端换了底(自实现 WSS 客户端替换第三方库,推送行为不变、风控恢复更皮实),并给皮肤系统的装饰残留收了尾。

### Changed

- **直播信息流客户端换底**:弹幕/醒目留言/上舰/开播下播的监听不再依赖第三方库 `blive-message-listener`,改用自实现的 WSS 客户端。对用户行为不变,底子更皮实:连接携带真实设备指纹(buvid3)、与 HTTP 请求同一浏览器指纹,更不易被风控;被风控(-352)拦下的房间进入长退避循环(60 秒起步、30 分钟封顶)自动重探,风控解除即自愈、永不放弃;建连/认证有 15 秒超时保护,连不上的房间快速换线重试;单条坏消息只丢弃自身,不再可能带崩整条连接 (9a50581, 8f64743, 59c9b9f, 57f2b1d, b164e49, a8af923, 963e7cb, 0aa8c97, 5763deb, 499d5ff, 481cbc9, 948bbd8)

### Added

- **直播协议漂移告警**:B 站调整消息字段导致某类直播消息解析失败时,日志会限流告警(首次 + 每累计 100 次)点名是哪类消息漂了,而不是静默丢弃 —— 此前 B 站把礼物消息整体迁到新协议时,旧库在大房间漏掉全部礼物且无人知晓,这个告警就是那次的教训 (bf9a186, 8103d6e)

### Fixed

- **皮肤包的守卫样式残留清理**:导入自己(≤0.7.0)导出的皮肤包会刷一排 pointer-events 警告 —— 系统把自己写进皮肤的兜底样式当成违规上报,且残留样式会一直跟着皮肤存来存去。现在兜底样式只在页面注入时生效、不再写进皮肤本体,导入旧包时残留即时清掉不再报警;顺带修掉两处:老格式/手改的皮肤文件(缺 modes 字段)不再被误判损坏而静默消失(连带「恢复默认值」一起失效),作者自己写的 `z-index:-1` 不再被当残留误删 (309d1ed, b6b5f7e)

---

## [0.7.0] — 2026-08-25

皮肤系统上线:整个 Dashboard 从此可以换装 —— 编辑器里逐项细调,或者进「皮肤工坊」让女仆一句话给你做一套;顺带女仆学会了自定义技能,AI 请求的抗抖动也补了一轮。

### Added

- **皮肤系统**:「系统」页新增皮肤区 —— 皮肤库、试穿预览、一键换装/脱下;深浅色**各自**一格皮肤槽,亮色穿一套暗色穿另一套互不打扰;皮肤打包成 zip 导入导出,可带壁纸与字体文件;误穿翻车有紧急脱装开关 (f518f4f, 783876f, a4b4491, bc3f91e, e407038)
- **皮肤编辑器**:抽屉里逐项细调 —— 颜色、玻璃(透明度/糊化)、圆角、阴影、字体栈、壁纸、聊天页配色、左栏宽度、动效(流光/光斑),整页**实时预览**;每套皮肤自带出厂快照,改崩了一键恢复默认值,与出厂一致的项标「(默认)」;还能把一个模式调好的参数一键套到另一个模式上 (dd16ce1, acfa4c0, 49e63e1, 463af30, a479d2f, 557643c, 76d379f, 77201cc, 39d7828)
- **AI 皮肤工坊**:聊天页新增「皮肤工坊」模式 —— 一句话生成整套皮肤、贴一张图按图配色出装、给现有皮肤直接上传壁纸;工坊里女仆可以联网查配色;日常聊天模式对皮肤**纯只读**,做皮肤只在工坊里发生;每个会话记住自己的模式与人格,中途不串 (db600a6, c6d967b, d4a6467, 58197bd, df86399, e2776d7, bbc0ea4, 86971e8, 1e4acd6)
- **皮肤自定义 CSS**:编辑器与工坊都能写少量自定义 CSS,选择器与属性走白名单清洗(挂点词表覆盖按钮/输入框/开关/选项行/导航/徽章/玻璃卡等全部控件),坏样式进不来、也藏不住界面 (d3308c1, 16a4a8e, 927809c, 2329655, a8338f7)
- **女仆自定义技能**:手写 `SKILL.md` 放进数据目录就是一条新技能;AI 页新增「技能」编辑器 tab 可以直接建与改;聊天输入 `/` 的技能菜单改读服务端技能库,自定义技能即写即用 (8cff038, 4375d42, 62bb524, 01def8c, 1f19df3)
- **聊天体验**:慢工具(如做皮肤)显示进度不再像卡死;图片可以直接粘贴进输入框;被截断的长工具参数可展开看全;输入框随内容长高 (24af7b5, a2e9532, c5c825d, 59cb490, 2bc4d4e)
- 推送历史的类型筛选从一排小胶囊换成分段选择器,选中态一目了然 (003709c)

### Changed

- **聊天页的四色预设移除**:聊天配色回归一套默认主题,想换观感请穿皮肤 —— 原来那四个色卡不再存在 (0e4d25f)
- 全站按钮/弹层/加载态收拢到统一组件,等待占位一律玻璃卡 —— 观感更一致,也是皮肤能够到每个角落的前提 (c9ff1f9, f49a1c3, 53ee110, 85a98e9, 28c6b08)

### Fixed

- **AI 请求抗抖动一轮**:撞到限流(429)按网关给的 `Retry-After` 时刻回来重试(超过 20 秒的直接报错不空等);流式回复给首块更长的宽限(思考型模型开场慢不再被误杀),中途卡死的流会被看门狗掐掉重试;超时不再降级成非流式把慢活白干一遍 (d59626d, 4f2a96a, e4bb101, 867a351, a1f10c1)
- **正则安全闸门放过了有界重复的嵌套构造**:动态黑白名单里写下 `(a{0,30}){0,30}` 这类正则会被保存放行,推送检测一撞上就把整个服务卡死 (0483929)
- **安全两条**:所有 API 响应补上 `nosniff` 头;AI 服务商的 API key 不再以明文经由前端状态通道往返 (dfe8aa1, 63f9f1f)
- 日志页的级别颜色在浅色界面上几乎分不出来,加深了浅色档(控制台恒暗面不动) (624160d)
- 顶栏导航点下立刻高亮选中,不再等路由切换的那一拍(像素风皮肤下会看到按下又弹起) (3b5f818)
- 吸顶顶栏会被页面内容盖住 (f25591b)
- 推送历史翻页时,同一天的条目可能被劈在两页边界上少显示几条 (7b352a2)
- 聊天进行中切走再回来,正在生成的会话可能凭空消失 (1eb53ef)
- 订阅页开着筛选时,「添加 UP」卡被滤掉会让整排 UP 卡塌缩变形 (8eb112e)
- 亮色主题的次要/辅助文字层级次序装反了(次要比辅助更浅) (342741c)

---

## [0.6.0] — 2026-08-14

私聊里能直接指挥女仆了(指令系统),Dashboard 添了独立聊天页(带思考过程直播),AI 学会了联网搜索,同一家服务商能存多份配置,还接上了 OpenAI 的 Responses 协议。

### Added

- **私聊指令系统**:私聊发「帮助」就能唤女仆干活 —— `status`(登录态 / 上次抓取 / 推送通道 / 渲染排队)、`mute <时长>`(**全局**静音推送,到期自动恢复,`3h` / `2小时` 这类中英文时长都收)、`report [天数]`(现拉一份锐评周报,按面板上的审批开关走)、`login`(重新扫码登录,二维码只发主人私聊)。英文指令名和中文别名都认,指令前缀 / 别名 / 总开关可在系统页配置,打错字会提示最接近的那条;系统页新增「指令」卡片列出全部指令与用法 (a0e35de, e325d15, 41cbaad, b7a2e5f, 43adc91, fa2e1a8, 4b4ae9f, a53b69c, 36e307b)
- **AI 聊天页**:聊天从侧栏抽屉搬进独立的 `/chat` 页。模型思考时能看到 DeepSeek 式的「先想后说」直播(思考稿不混进正文);输入条重排成 DeepSeek 风格,图片和技能并进一个 + 按钮;思考开关 / 深度和联网搜索做成**会话级**胶囊,切会话各记各的 (f8c316b, 6ffc72c, 20eb499, 8151f2c, d910f7c, bd86445, 531c730, 98bad0e)
- **AI 联网搜索**:AI 页新增「联网搜索」设置块 —— 搜索由**博查**或 **Tavily** 真正执行(`web_search` 工具),跟选哪家 AI 服务商无关,DeepSeek 官方也能联网。聊天页按会话开;动态点评 / 直播总结 / 锐评各有独立开关,**全部默认关**(搜索按次计费)。两家 key 各存一格、换后端不丢;key 走密袋存储,备份导出自动脱敏。聊天里每次搜索完可展开「来源列表」看她引了哪几条 (01df44c, 63cb0a5, 80aad8c, 77dc580, ebec23e, 8418b7b, e492993, 70a5d3e, 23451a8)
- **同一家服务商可存多份配置**:AI 服务商从「一家一份」变成「一家多份」—— 同一家开多个 key / 模型组合,随时切换当前生效那份。老配置自动迁移;**旧版备份**恢复进来也能正常带回 AI 密钥 (846a28f, 2fac417, 13b5bb7)
- **阿里云百炼(DashScope)服务商**:服务商列表新增百炼,思考开关 / 深度按它家 `enable_thinking` 的写法自动翻译 (5395655)
- **Responses API 接口风味**:每份 AI 配置可选走 `chat`(经典 `/chat/completions`,默认、行为不变)还是 `responses`(OpenAI 2025 起的接任协议;DeepSeek 已生产级支持、百炼 qwen3-max 系可用、OpenRouter 还在 beta)。responses 下思考是协议标准字段 —— 「自定义」服务商也能开思考,OpenAI 官方正是这么接的;DeepSeek 上关思考会显式发 `effort: none`(它家默认是开的) (9dc5250, 024e9a1, d7f2e39, d59fa36, 9d2d700, ce956c6)

### Changed

- **桌面版启动页重写**:改成与 Dashboard 统一的浅色玻璃风格(原来是一页随手写的暗色卡片)。行为不变 —— 服务就绪自动进面板,重试 / 日志 / 数据目录 / 程序坞按钮都还在原地 (ba62103)
- 桌面图标的图形做了视觉居中并放大一圈 —— 原图形在方块里偏上且偏小 (ab0db6c)

### Fixed

- **DeepSeek 开思考时一调工具就 400**:DeepSeek v4 要求把思考内容原样回传给工具调用的后续轮次,流式路径把这个字段丢了 —— 「边想边查」(查订阅 / 看图 / 联网搜索)的回合必炸 (648487d)
- **秒级断流重开时推送次序倒置**:主播掉线几秒又重开,新开播卡可能抢在还在路上的下播卡前面送达,群里看到「开播 → 下播 → 总结」,下一次整点续推读起来像幽灵开播。同房间的推送现在按发起顺序串行送达;慢工序(词云 / AI 总结)不会拖住重开的开播卡 (abf308b)
- **引用回复里的「y」认不出来**:锐评审批回复带引用时,OneBot 的 `raw_message` 会把它包成 `[CQ:reply,…]y`,y / n 怎么回都不认。现在优先读消息段数组,`raw_message` 只作兜底并解转义 (305082e)

---

## [0.5.3] — 2026-08-08

定时 AI 锐评落地:到点自动把榜单周报或某位 UP 的个人锐评发进群,发之前还能先私聊给主人过目、等一句「y」再放行。另外镜像的 Node 堆上限抬到了 512MB,并新增一条内存自检日志 —— 有人撞到了旧的 384MB 天花板,而光凭崩溃日志分不清是本来就该吃这么多、还是在漏。

### Added

- **定时 AI 锐评**:「全部 UP 的榜单周报」和「单个 UP 的个人锐评」各有一条独立的定时线,各自设 cron、各自选发送目标,到点自动生成并推送。统计范围 7 / 30 / 90 天,与数据统计页上面那排口径一致。榜单那条在数据统计页顶部设置,某位 UP 的那条在切到该 UP 之后设置 (f811172, 19d0e47, 5245f74, 357fc54, 5e0cb69, 6ee2ce2, f81f1a0)
- **发送前先给主人过目**:打开审批后,锐评生成完先私聊发给主人 —— **主人看到的就是群里将要收到的那一份**(卡片图开着就连图一起发,不是另写一段摘要),回「y 编号」放行、「n 编号」丢弃,48 小时没回自动作废 (4fa6c36, f1b3c68, 8d94b32)
- **主人可以直接在 QQ 里回 y / n**:OneBot 与 QQ 官方两条通道都能收主人的私聊回复了。webhook 只有出站没有回程,在它上面开审批会被直接拒绝并说明原因 —— 否则那条锐评会一直等一个永远收不到的回复 (c21d1a3, 5a133a1, ca3510a)
- **「试一次」按钮**:不用等到点,立刻按**已保存**的配置跑一轮,走的就是 cron 到点走的同一条路(所以顺带也验了「回 y」这条路通不通)。⚠️ 审批关着的时候它是**真发进群**的,不是演习 —— 按下去之前会先问一遍,并写明会发给几个目标 (8e91e60, 1288255)
- **内存自检日志**:默认每 10 分钟一条 `[mem]`,报堆用了多少 / 离上限多远 / 常驻内存 / 堆外内存,以及弹幕收集器占着的规模。堆用量超过上限 85% 时升为警告并附处理办法。嫌吵可以设 `BN_MEMORY_PROBE_SECONDS=0` 关掉,不必去动日志级别 (195ca5e)
- **动态检测出故障时会直接私聊主人**:账号被风控、或拉取动态报错时,除了原有的告警面板,现在还会私聊说一声;**恢复正常之后也会私聊报个平安**(这条只私聊,不会去点亮告警面板) (a4927ba)
- 动态里带的关联视频,现在会渲染成视频卡片,而不只是一行链接 (9c31d0f)

### Changed

- **Docker 镜像的 Node 堆上限从 384MB 抬到 512MB**。有用户订阅了十几个 UP 之后撞到旧的天花板,日志里是 `FATAL ERROR: Reached heap limit`。⚠️ **这是抬余量,不是根治** —— 到底是稳态就需要这么多,还是有地方在漏,目前还没定论,上面那条 `[mem]` 日志就是为了把这条曲线记下来好回答它。仍可在 compose 的 `environment` 里设 `NODE_OPTIONS` 覆盖。另外注意 `mem_limit` 要按**常驻内存**配而不是按堆上限,堆外那部分不受这个数管 (901cbba)
- 桌面版的应用图标改成苹果的 squircle 规格,在 macOS 程序坞里不再比邻居大一圈 (91a7ef1)

### Fixed

- **AI 聊天在刚填完 apiKey 之后仍然说「baseUrl / apiKey 还没填齐」**,而且只有重启才好。填完 key 其实已经把实例建起来了(日志里都写了「commentary 已激活」),动态点评和直播总结也确实拿到了新实例,唯独聊天这条路拿的还是启动那一刻的空值 (16d8dcf)

---

## [0.5.2] — 2026-08-02

这版几乎全在收拾上一版留下的尾巴,大多是内存与错误处理这类看不见的地方:上传的字体不再在内存里存两遍、也不再赖着不放,超大文件在读进内存之前就被挡在门外,卡片预览排队发出、一张卡卡住不会再把其余几张拖死,出错时也不再把前端自己的毛病说成「网络断了」。另外 OneBot 多了两栏超时设置,带图和合并转发的超时终于能自己调。

### Added

- **OneBot 的超时可以按消息类型自己调了**:适配器设置里多出「带图超时下限」与「合并转发超时下限」两栏。带图消息要等协议端把图传上 QQ 图床才回响应,比纯文本慢得多,所以这两类的超时是单独放宽的 —— 带图 30 秒(这版新加,原先只有合并转发有 60 秒)。现在这两个数写在界面上、调得动,**填 0 就是关掉**,严格按你填的超时走(为快速故障转移特意把超时调小的人,不会再被它静默无视)。⚠️ 只影响独立端的 OneBot 适配器 (7fb21a9, 2c1cab6)
- **上传字体过大时会提醒**:超过 8MB 的字体会提示转 woff2,因为出图时它会整份进内存。这条提醒**按当前选中的那款算** —— 换一款就跟着变,刷新页面也还在,不是上传那一下弹一次就没了 (6d8ccd8, a94e195)

### Fixed

- **上传的字体会一直占着内存不放**。两个毛病:同一份字体在进程里存了两遍;把卡片切回默认字体之后,那几十兆也不会被放掉。现在整条链上只留一份,并且闲置 5 分钟后自动释放 (cca9764, 695ba15, 44b85a3)
- **卡片预览会一起堆在那儿转圈**,而且转半天之后报的错常常驴唇不对马嘴。现在预览排队发出、单个请求有 120 秒死线,一张卡卡住不会再把其余几张一起拖死 (da4d7e8, 0d64e14)
- **面板报错会指错方向**,有两处归错了类:一是断线判定靠捕获 `TypeError`,可组请求头、序列化请求体也会抛它 —— 那时请求还没发出去,却会被报成「连接中断」并劝你去调反向代理;二是响应读到一半失败会被静默吞掉、当成「成功但内容为空」返回,页面随后炸一句 `Cannot read properties of undefined`。现在组请求的活儿挪到断线判定之外,成功响应没读全会如实说「响应不完整」(6a6da43)
- **超大文件是先整份读进内存、才发现它超限的**。字体、背景图、聊天配图三处上传都这样 —— 文件多大就先在内存里占多大,那句「文件过大」要等它读完才来。现在在读进来之前就按体积挡掉,直接回 413 (695a3dd)
- **AI 点评和直播总结会空转**:回一句「我得先确认一下有没有订阅这个 UP 主,稍等一下哦!」然后就没有然后了。那条路一个工具都没挂,提示词却在要求它调工具 (5aa1973)
- **群聊女仆会声称帮你改好了订阅,其实什么也没发生**。订阅的增删改早就只能在面板或指令里做,提示词却还在指挥她去调用并不存在的工具 (4c9e747)

### Changed

- 字体上传被拒时的提示,从一行小红字改成和「字体偏大」同样醒目的提示块 (12ab29a)
- 部署文档补上两条建议:出图那一下是整个服务的内存峰值,**宿主可用内存建议 1GB 以上**(只有 512MB 的请改用 `:slim` + 远程浏览器);经反向代理访问时**读超时请调到 120 秒以上**,否则渲染成功了请求却被代理掐断,两边对不上很难查。另外附了两条 `docker` 命令,用来分辨面板断线到底是被 OOM 杀过、还是代理掐的 (5a6b449)

---

## [0.5.1] — 2026-07-31

卡片字体从「手填 font-family」换成了选择器,并且可以直接上传自己的字体文件。多图动态跟着 B 站官方改成九宫格,GIF 也终于标出来了。修掉的几条是同一类毛病 —— 界面上选得动、也存得住,就是不生效。

### Added

- **卡片字体选择器**:原来那个手填 `font-family` 的输入框换成三档 —— 默认、你上传的字体、手填家族名(高级)。**可以直接上传字体文件**(单款 20MB 以内,建议用 woff2);删除时若还有地方在用,会告诉你是谁在用 (6922d59)
- **多图动态最多铺 9 格**,与 B 站网页端一致,多出来的折进第 9 张图上的 `+N`。以前是有几张铺几张,十几张图的动态能把卡片拉得极长 (529b55c)
- **GIF 会标上「动图」角标** —— 推出去的是截图,只截得到一帧,不标的话看着就是张静图 (529b55c)

### Fixed

- **切换全局人格不算数**:智能女仆聊天窗的名字与口吻、「试一句」、统计页的锐评、人格选择器上方那行介绍,换了人格之后全都还念着旧的 (d11c1be)
- **给单个 UP / 单类推送单独挑的字体从来没用上**,挑了等于没挑 (6922d59)
- **卡片字体设置从来没生效过**,填什么字体名都不作数。⚠️ 现在生效了,升级后卡片字体可能真的变了 (6922d59)
- **含 GIF 的多图动态推送时会凭空少几张图**,而且不报任何错 (78a8b5d)

### Changed

- 仪表盘首屏小了约 82 KB (c78e1bf)

---

## [0.5.0] — 2026-07-28

智能女仆这版从「底部一条建议栏」变成了完整的聊天界面 —— 能连续对话、能发图、回复带 Markdown 排版,会话自动命名并留档。AI 设置页同时重做成三个 Tab 加一个人设库,并新增**图片理解子模型**:主模型不支持看图(比如 DeepSeek)也能让女仆读懂图片。另外仪表盘顶部的 Tab 现在可以自己挑、自己排序。

升级前请注意 **Changed** 一节:群聊女仆的订阅管理工具被整体移除,「联网搜索」开关不再存在,全局人设的存储方式也变了。

### Added

- **智能女仆聊天界面**:替代原来的底部建议栏。流式回答(SSE)、会话持久化、切换会话不丢上下文;回复按 Markdown 渲染;可以往对话里**贴图**;女仆调用了哪些工具会显示出来;新会话的标题由女仆自己拟,而不是截取你的第一句话。聊天面板的玻璃透明度可调,强调色跟随主题 (f3c0cee, a9a1c86, dd93181, 43d1194, 07f8c85, 97dbaa4, d2d4367, fc7b15a, 22cc7f5)
- **图片理解子模型**:配一个专门的视觉模型,它先把图片转成文字描述,再交给主模型。这样**主模型本身不支持看图也没关系** —— DeepSeek 官方接口里一个视觉模型都没有,以前这条路是彻底堵死的。子模型有自己独立的 baseURL 和 API Key(视觉模型通常在另一家)。群聊女仆则拿到一个 `describe_image` 工具,由她自己决定哪张图值得看 (e30d235, f9ff62b, 52d4c15, 1c6a65b)
- **AI 设置页重做**:拆成三个 Tab,并带一个**人设库** —— 人设作为条目集中管理,可新建、可克隆内置的再改。每个 UP 的单独 AI 设置也改成**从库里挑一个**,不用再逐个字段手写一遍 (cd9a2c4, e900046, cc406d0)
- **按供应商分别存配置**:每家供应商各自保留一整套档案,**换供应商不用重新输 Key**。深度思考按供应商翻译成它认识的拼法,另有一个自由参数框覆盖没适配的厂商。供应商由你明确选择,不再从 baseURL 猜 —— 猜错就意味着把 A 家的参数发给 B 家 (753ae90, e30d235, f5c4295)
- **仪表盘 Tab 可自选与排序**:顶部导航显示哪些 Tab、按什么顺序排,自己定 (b96ce74)
- **默认文案更新提示**:我们改了某条默认推送文案时,你会在对应字段上看到提示,并可以选择采用新的或保留自己的 —— 以前改了默认文案,已经装过的人是永远拿不到的。判据改成「你有没有见过这一版默认值」,手写过文案的人不会被一条永远消不掉的提示骚扰 (95c8a8d, 21644b7, dc963ff)

### Changed

- **全局人设改为指向人设库中的一个条目**。以前切换人设是把内容**复制**进全局配置 —— 会直接覆盖你手写的那份,而且撤不回来。现在存的是一个指针,切走再切回来,手写的原样还在。内置的四个人设锁定不可编辑(想改就克隆一份)。另外内置的「analyst」被重写成女仆理子:原来它是四个里唯一一个不是女仆的,称呼、自称都和其它三个对不上;它的 id 没变,所以**每个 UP 已经指向它的设置继续有效**,只有全新配置才会拿到新文案 (5faf811, 0bbb9f6)
- **每位 UP 的单独人设改为「从人设库里挑一份」**,设置页当年那档「完全自定义」(就地给这位 UP 写死一套人设)已经撤掉,人格一律在「智能女仆」页里统一维护。**升级前用那一档给某位 UP 手写过人设的,那一份不再生效、落回全局人设** —— 继续读它就成了界面上看不见、实际仍在生效的鬼配置。设置页会把这位 UP 的开关显示成「关」,界面和实际行为是一致的。想找回来的话:把那套人设在人设库里新建一份,再到这位 UP 的设置里指过去 (e900046, 5faf811)
- **群聊女仆的订阅管理工具已整体移除**,现在只剩只读工具。原来女仆能帮人订阅/退订 UP,但那条路径**完全没有权限校验**,而且写入只落在内存里、每次重载配置就被冲掉 —— 是个看着能用、实际留不下的能力。如果你在群里靠这个加订阅,请改用面板或指令 (e30d235)
- **「联网搜索」开关已移除**。各家供应商的拼法差得太远,统一不了;需要的话用新的自由参数框自己写 (e30d235)
- **「深度思考」此前从未真正生效过**。参数被塞在 `extra_body` 里发出去 —— 那是 Python SDK 的用法,Node 客户端会把它原样序列化成一个没人认识的字段。现在参数放到请求体顶层,并按供应商翻译(OpenRouter 用 `reasoning`,火山与 DeepSeek 用 `thinking`,SiliconFlow 用 `enable_thinking`)。也就是说,**开着这个开关的人,从现在起才第一次真的用上它** (e30d235)
- 统计图表在缺数据处画成灰色而不是留白,曲线的断开更容易看出来 (19c3e01)

### Fixed

- **登录掉线后,没人扫码却自己重启了一遍**:掉线瞬间有一次探活请求还在路上,它带回来的「登录正常」把状态推回了已登录 —— 于是动态检测重启、直播间重建、关注重试,全部再撞一次 `-101`,并且过一会儿又推一条重复的「账号登录已失效」。现在会话一死,在途请求的结果一律作废 (4cb7bbc)
- **聊天里选好的图片发不出去**:界面上明明挂着附件,服务端一张都没收到 (c1537eb)
- **API Key 在草稿岛的改动对比里以明文显示** (a8719bc)
- 保存 AI 配置时,上一条回复会闪一下再消失 (f53a84a)
- 点「新建会话」但没说话,会留下一条空记录 (f9e19c4)
- 统计页的对比表列按维度分组,粉丝趋势图改用累计值,热力图在 UP 加入订阅之前留空而不是画成 0 (1e735f0, f23f2d2, 7b40677)
- 草稿岛的错误标记没有居中 (2495c7e)

---

## [0.4.0] — 2026-07-22

新增「数据统计」Tab —— 记录并展示每位 UP 的涨粉、投稿、动态与直播活动,还能让智能女仆据此写一份周报或单人锐评、直接推成卡片图发到群里。另修复四个正确性问题,其中「配置里关掉的开关关不掉」与「直播间卡在正在直播」影响面较广。

### Added

- **「数据统计」Tab**:记录每位订阅 UP 的涨粉曲线、投稿 / 动态数与直播场次时长,提供对比表、KPI、活动热力图、雷达图与单 UP 钻取,时间窗可选 1–90 天。数据全部来自**已有的**事件流,不额外请求 B 站。

  两件事值得先知道:一是**统计从升级这一刻才开始记**,升级之前的活动没有留档,所以头几天的图表会比较空;二是**「没有记录」与「确实是 0」严格区分** —— 服务器当时没在跑、或功能还没上线的日子一律显示成空格而不是 0,曲线在缺数据处断开,不会画一条看着像「那天没动静」的实线 (7890649, 79beb6e, 6c356dd, 17d9e15, 0447fe7, 464ea77, 9bc2935, fef42aa)

- **AI 周报 / 单人锐评可直接推成卡片图**:统计页的「可推送周报」此前只能看不能推,只好手工复制;现在可以选一个推送目标直接发出去,榜单卡(鸽王 / 勤奋 UP / 逐位锐评 / 评分条)与单人锐评卡各一张。

  没装 Chromium 或关掉了图片渲染也发得出去 —— 会依次降级成纯文字,而不是整条失败。推送的是**你在页面上看过的那一份**,不会在服务端重新生成一份没人审过的文本 (ecadf03, 05a62a4, 0390fec, e127fa0, 8ff7ab0, d218329)

### Changed

- 界面与卡片里的 **emoji 全部换成 SVG 图标**。emoji 的长相由观看者的系统字体决定,同一个警告图标在 macOS 是黄色三角、在 Windows 是另一副样子、字体缺失时干脆是豆腐块;卡片图更明显,渲染机器的字体不同,推出去的图就不同 (673dc75, e962b49)
- 每位 UP 的**代表色从 8 种扩到 24 种**。原先 8 种里有两种肉眼几乎分不出,实际可用只有 7 种,订阅到第 4 位就有约六成概率撞色、10 位以上几乎必然撞 (88decb1, 0bfdca2)
- 顶栏那枚绿色徽章由「推送服务运行中」改名为「**服务器运行中**」。它只表示后端能不能连上,不少人把它当成推送的总开关去找 —— 推送开没开是每位 UP 各自的设置 (727ab9b)

### Fixed

- **配置里关掉的开关关不掉、清空的字段清不掉**:典型表现是「打开直播卡的单独样式、保存,再关掉就关不掉了」—— 请求成功、页面也没报错,刷新回来开关又是开着的。同类问题还有:清空 AI 的 API Key(换服务商时要做的第一件事)旧密钥仍留在服务器上;模块日志级别在有两个以上覆盖时改不回「跟随全局」;按 UP 的单项卡片样式在开了两种、只关一种时关不掉。所有保存路径已统一走同一套差异计算,不再各写各的 (e8b93ba, 4bd3776, 440f934)
- **账号登录失效后,动态检测会再报一遍错**:主人先收到「账号登录已失效,请到控制台重新扫码登录」,过一会儿又收到一条「[bilibili-notify-dynamic] 账号未登录」,读起来像是又坏了一处。现在动态检测跟直播监听一样在登录失效时立刻停下,不再多打一次注定失败的请求;日志措辞也说明了原因和恢复条件 (e991a0a, 834fd68)
- **直播间可能卡在「正在直播」下不来**:有四条路径会在等待网络期间翻转直播状态,其中两条没有防护 —— 下播事件恰好落在这个窗口里就会被丢弃,而 B 站不会再补发一次,该直播间从此永远显示直播中。附带的三个后果:重连放弃时会留下一个每小时私信一次、永不停止的提醒;开播中断时上一场的弹幕会漏进下一场的词云 (4859656)
- **冷启动时 cookie 已过期的话,重新扫码登录后不会自动恢复**:动态检测、直播监听重建、关注同步全都停在原地,必须重启服务才恢复。这个场景下的登录失效走的是另一条状态迁移,恢复事件被漏发了 (5efddc9)

---

## [0.3.0] — 2026-07-17

渲染浏览器支持远程连接与不重启热切换,配套推出免装 Chromium 的 slim 镜像并收紧容器常驻内存;直播开播卡片封面可换成自选图;修复图集合并转发重复推送与图廊悬空引用两个正确性问题。

### Added

- **渲染浏览器可远程连接、支持不重启热切换**:系统页新增面板,可把卡片渲染指向远程浏览器端点(不再要求本机装 Chromium),切换来源无需重启服务;配套推出免装 Chromium 的 **-slim** Docker 镜像(需搭配远程浏览器容器使用),本地 Chromium 空闲一段时间后自动关闭以降低容器常驻内存,两种镜像都收紧了 Node 堆上限 (96ca28a, 0ab2798, 66e10f4, 15f8978, ddaf57d, b1374f7)
- **直播开播卡片封面可换成自选图**:卡片编辑器新增「直播封面」区块,全局与按 UP 单独设置皆可,可选多张轮换;不选则保留 B 站原始封面 (0bbef92, 3996a1f, be74c8d, f64e5dc, 39a0580)

### Fixed

- 动态图集(合并转发,多图)推送**超时后被误判为失败并重试,导致同一条动态在群里重复出现多份** —— 超时不代表消息没发出去,现在只对确定没发出去的失败重试;合并转发的超时阈值也放宽到 60 秒以适配多图下载耗时 (32c2d0e)
- 卡片图廊里引用了一张已删除的图片时,该图会**悄悄占住轮换位却在界面上不可见、也无法取消选中**。现在失效引用会显示成可删除的「已失效」占位块,删除一张图片时会一并清理页面上所有未保存草稿里对它的引用,渲染与推送侧也会直接跳过失效图片 (3bfe551)

---

## [0.2.1] — 2026-07-15

AI 页新增「试一句」实时测试面板;订阅时自动关注 UP、并补关注历史上从未关注的订阅;外加一批推送正确性与界面修复。

### Added

- AI 页新增 **「试一句」面板**:用当前(哪怕还没保存的)人格草稿,当场发一句话给智能女仆,她的回复会同时显示在页面上、并真实推送到你指定的推送目标 —— 调人格时不必再跑去聊天软件里翻效果 (5841068, b9becc4, aa160b1)
- 订阅卡片上显示 **「未关注」标记**,一眼看出哪些订阅还没关注(未关注的 UP 拉不到动态) (618317e)

### Fixed

- **订阅一个 UP 时自动关注他**,并在启动时**补关注那些历史上从未关注的订阅** —— 动态是从关注流拉的,不关注就收不到该 UP 的动态。此前独立端从不自动关注,存量订阅可能一直静默收不到动态 (d1a00d0, 0191fbf, 9a7a50e)
- **「特别关注用户进房」提醒终于生效** —— 这个功能此前依赖一份不存在的解码表,每次都静默失败,从第一天起就没工作过。现在改用直播消息组件自带的进房事件 (ba9914b)
- 给某个 UP **单独改一项卡片样式**(例如只改渐变起始色),会把该 UP 的字体、背景图轮换、粉丝区显示等**其它全局设置一并冲回默认** —— 最严重的是全局关掉的图片渲染会被悄悄重新打开。现在单项覆盖只改那一项 (9c1c476)
- 图文动态(多图帖)的**附图发送失败时,主卡片连同 @全体会在之后每一轮检查里重复推送**,直到那条动态滚出列表。现在附图发不出就跳过,不连累主卡重发 (808b480)
- AI 页**只改了 apiKey 一栏、保存之后仍卡在「未保存」状态**,再也点不动保存 (1145c09)
- 配置热重载会**跨功能域误触发**:改个动态轮询设置会顺带重置 User-Agent、重排登录健康检查计时;更糟的是**图组转发开关改了之后不重启根本不生效**。现在按字段精确判定,各设置改动只唤醒真正相关的部分 (aeb64eb)
- 停止监听某个直播间时若撞上开播推送正在进行,会残留一个永不停止的「正在直播」复推定时器,对着已取消的订阅一直推。现在拆除时一并清掉 (aa387a0)
- 账号被风控、退避等待期间增删 / 开关订阅会提前重启动态检测、抢在退避结束前又去戳风控接口。现在退避期间不再被订阅变更打断 (55af1ec)
- 请求出错时错误日志会带上完整请求 URL,其中可能含账号 CSRF 令牌等凭据,报 bug 贴日志即泄漏。现在日志只保留路径 (742af30)
- 修复六个失效的界面颜色 token(部分控件颜色渲染不出来) (5f19ab3)

### Changed

- **默认**动态轮询时间从每 2 分钟的整分(:00)错开到第 30 秒(:30),避开整分请求高峰、降低风控概率。你若自己设过 cron 表达式则不受影响 (d2321ef)

---

## [0.2.0] — 2026-07-11

新增一键备份与恢复(完整备份 / 脱敏导出两档,恢复不需重启),订阅卡片支持右键 / 长按快捷菜单。修复一类「可选项改回默认值就存不下去」的保存故障,以及脱敏备份在配置有 QQ 官方机器人时**根本恢复不了**的问题。

### Added

- **备份与恢复**(系统页新增一节)。两档:**完整备份**把 B 站登录与 AI API Key 等机密用 6 位 PIN 加密后一并带走,换机 / 重装能原样恢复,连登录都不用重扫码;**脱敏导出**把所有机密位置留空,可以放心分享给别人。导出时按「全局设置 / 订阅 / 推送目标 / 适配器」分节勾选。导入分覆盖与合并两种模式,**覆盖前会先算出计划、把「将删除 N 项」摆到你面前确认**(删除不可撤销)。恢复后配置热重载,不需要重启 (72e1a9b, 0ae2ed8, b7c0fe6, 7653b12, c41d0be, 63313ca, c7dbaea)
- 订阅 UP 卡片支持右键(桌面)/ 长按(触屏)呼出快捷菜单:编辑详情、启用停用、复制 UID、加入分组、删除 (195d147)

### Fixed

- 「玻璃片透明度」这类可选项**改回默认值后存不下去** —— 改动会显示成「→ undefined」,保存点了没反应。现在清空一个可选字段会正确地作为「清除」提交。同类问题在卡片全局样式、per-UP 覆盖、日志级别覆盖上一并修掉 (974ed46)
- **脱敏备份恢复不回来**:配置里若有 QQ 官方机器人适配器,导入直接报 `config validation failed`。脱敏会把 appSecret 抹成空,而它此前不接受空值 —— 也就是说这个功能对用 QQ 官方机器人的人从第一天起就是坏的。现在空密钥表示「尚未填写」,可以正常存下;恢复后该适配器不会拿着空密钥去撞 QQ 网关,导入回执还会提醒你回去补填凭据 (7b6bd91)
- 批量启用 / 停用订阅之后,选中状态没有清空 (7acbdd1)
- 在卡片渲染页拖动透明度滑块时,日志会一条接一条刷出 INFO「配置已更新」,看着像是已经保存了 —— 那其实只是预览在重渲染,已降为 debug (89ca0ca)
- 日志把「清空的可选样式字段」打印成 `undefined`,现在显示为 `(默认)` (5c8714f)

---

## [0.1.0] — 2026-07-09

首个正式版,从 alpha 系列毕业。Docker 镜像大幅瘦身(自包含 bundle + chromium 基础层冻结,升级只需拉取几十 MB);修复 per-UP 高级规则「动态过滤」与「直播阈值」开关互相连带点亮的问题。

### Changed

- Docker 镜像瘦身:服务端改为自包含单文件 bundle,镜像不再携带完整 node_modules,应用层体积从数百 MB 降到约 15MB,升级时只需重新拉取这一小层 (c291631, 3359e7f, 0f766a7, d6e5aa3)
- Chromium 运行时层(约 300MB)冻结进独立基础镜像,不再随每次发版重建;今后升级只需拉取应用层,chromium 层只在显式重建基础镜像时才变化 (6d5c97d)

### Fixed

- per-UP 高级规则「动态过滤」与「直播阈值」两个开关此前共享同一份底层字段,只开其中一个另一个也会连带点亮(侧栏小点同样误标),关闭后保存草稿灵动岛还会残留;已按字段分域拆开判定,两个开关互不干扰 (e4d11ce, 8e62307)
- 服务端入口通过符号链接路径启动时(如全局链接的可执行文件、部分容器挂载路径)会静默不做任何事直接退出,不报错也不留日志;已在入口比对前对路径同样做 realpath 解析 (8e4dc6f)

---

## [0.1.0-alpha.17] — 2026-07-04

底层网络传输从 axios 全面切换到基于 fetch 的实现(自带轻量 cookie jar、每实例一致的浏览器指纹),并配套一批降低 B 站风控概率的优化(粉丝轮询解耦降频、风控熔断、请求去重复用);另修复概览页「本周推送趋势」柱状图左侧空柱与「今日推送」计数在高推送量下偏低的问题。

### Added

- 粉丝数轮询独立周期 `fansCron`(默认每 10 分钟):从动态轮询周期(`dynamicCron`)解耦、可单独调节。订阅数量多的用户不再每 2 分钟对每位 UP 各发一次请求,请求频率大幅下降,降低风控概率 (21b0280)

### Changed

- 网络传输层从 axios 切换到基于 fetch 的实现:自带一个轻量 cookie jar(持久化格式与旧版兼容,升级不掉登录),逐跳捕获 Set-Cookie;并为每个实例生成前后一致的浏览器指纹(User-Agent 与 sec-ch-ua 版本互相咬合),减少指纹错配招致的风控 (f6c3d2a, 0762c2f, ff12670, db1c068)
- 一批降低 B 站风控概率的优化:粉丝数改用更轻量的关系接口拉取、命中风控时自动暂停本轮轮询而非继续请求剩余 UP、直播间号解析结果与登录账号信息缓存复用不再重复请求、WBI 签名请求持续被风控时不再快速重试放大 (8917753, 21b0280, 92188da)

### Fixed

- 概览页「本周推送趋势」柱状图左侧总有空柱:数据源此前只取最近 100 条推送,推送量大时覆盖不满 7 天、越早的日子分到的记录越少直至为零;现改为服务端按日全量统计,并按本地时区(而非 UTC)划分日界。「今日推送 / 今日失败」计数同源修复(单日推送超 100 条时不再偏低)(4f7ebd6)

---

## [0.1.0-alpha.16] — 2026-07-03

消息版式自定义(一次推送可拆分 / 重排为多条消息)+ 充电专属动态占位渲染;修复全局默认背景图廊不轮换、镜像更新后面板仍是旧版的浏览器缓存问题。

### Added

- 消息版式自定义:动态与直播(开播 / 直播中 / 下播)推送的消息结构可视化编辑 —— 卡片图 / 文本 / 链接三个部件可排序、显隐,插入分条符把一次推送拆成多条消息,同条内相邻文本部件的连接符可自定义;全局与 per-UP 均可配置。编辑器带实时预览,并在卡片图不在消息首位时提示 QQ 会把图文拆成两条 (e669a9d)
- 充电专属动态:未充电时 B 站接口不返回动态内容,此前会渲染出正文空白的卡片;现渲染「充电专属」占位提示 (54fb032)

### Fixed

- 背景图轮换对全局默认图廊不生效:此前仅当订阅或卡片类型单独配置了多张背景时才轮换,只在全局默认样式配图廊的用户每次推送都是同一张;现全局默认图廊同样参与轮换 (ce97b1f)
- 镜像更新后面板仍是旧版:Dashboard 静态资源此前不带缓存头,浏览器会按启发式缓存旧页面,表现为「核心版本更新了但面板没更新」,需强刷或删镜像重拉才恢复;现带正确缓存策略(哈希资源永久缓存、入口文件每次回源确认)。升级到本版后首次访问仍需强刷一次,此后镜像更新面板自动跟随 (6ce4f99)

---

## [0.1.0-alpha.15] — 2026-07-02

一批稳定性与体验修复:卡片版式全局热更新补漏、独立端对畸形 cron 表达式的崩溃防护、推送路由在退避重试期间的陈旧目标复检、QQ 官方机器人连通性测试改为真实探测并新增重连日志开关。

### Added

- QQ 官方机器人适配器新增「记录重连日志」开关(默认关闭):网关 RECONNECT/RESUMED 事件约每 30 分钟一次、属正常协议行为,此前无差别刷屏,现默认静默,排障时可开启 (2141cf6)

### Fixed

- 卡片版式(块顺序 / 显隐 / 间距)只改全局版式时,已缓存的直播 / 动态订阅快照不刷新,实际推送仍用旧版式(预览始终正确,只有真实推送滞后) (32113d9)
- 独立端下播轮询周期(`app.dynamicCron`)填入无法解析的表达式会在启动时直接崩溃进程,且畸形值一旦写入磁盘,每次重启都会复现崩溃;现已加运行期防御 + 保存前校验双重兜底 (514e719)
- 订阅推送目标退避重试期间(如 OneBot 断线重连中),若用户此时把该目标从订阅里移除,重试仍会在目标恢复可达后照常发出;已取消的推送目标不再在重试窗口内复活 (0f45425)
- QQ 官方机器人连通性测试:此前基于网关 WS 在线状态判定,首次启动、握手未完成时会误报「网关连接中/失败」(实际 REST 推送通道正常),且延迟恒显示 0ms;现改为真实探测 REST 通道 (2141cf6)
- 卡片渲染配置变更日志:此前无论实际改了哪个字段,一律打印渐变色,容易被误读为改动没生效;现只打印真正变化的字段 (203a7fb)

---

## [0.1.0-alpha.14] — 2026-06-29

卡片渲染系统大升级:可视化版式编辑器、每卡片类型单独样式、背景图廊与背景轮换、玻璃片透明度,以及为单个 UP 主单独覆盖样式 / 版式 / 数据区;另含一批推送与渲染修复(动态图集重复 @全体、冷启动渲染竞态等)。

### Added

- 卡片版式编辑器:开播 / 动态 / SC / 上舰四种卡片的版式现在可视化编辑 —— 块的顺序、显隐、块间垂直间距、插入分割线,所见即所得;全局与 per-UP 均可配置(旧版保存的版式自动迁移补齐每块间距) (264ede0, bef253a, e0b0faf, 9aaf7d7, 8285b0d)
- 每卡片类型单独样式:开播 / 动态 / SC / 上舰可各自设置独立样式(渐变色、背景图、玻璃片等),未覆盖的类型继承全局基准 (6e4d8d2, c36a874, 1c8a0e9, 24d88f3)
- per-UP 卡片覆盖:可为单个 UP 主单独覆盖卡片样式与版式,叠加在全局之上;不覆盖则继承全局 (3965102, 5d01f4e)
- 卡片背景图廊:上传自定义背景图、多选图廊管理、删除时保护仍被某卡片样式引用的图;背景以列表存储,存量单图配置自动迁移 (66be478, 1818cf8, 360170e, 1e869be, aa6b356, 3c1511b)
- 背景轮换:为某类卡片配置多张背景时,每次推送轮换使用下一张(开播 / 动态 / SC / 上舰各自独立游标) (b95ae44, 3807e16, 2ac3c45)
- 玻璃片透明度调节,以及「完全透明」开关(去磨砂模糊、底图清晰透出,与透明度互斥) (d550ea2, 39a95d9)
- 直播卡「数据区」开关:用统一的数据区控制取代原先零散的隐藏标志,可整体控制人气 / 分区 / 粉丝数等的显隐,全局与 per-UP 均可配置 (b80ed0b, f822dbd)
- 卡片预览增强:per-UP 作用域用该 UP 的真实数据渲染预览(开播按 uid 拉取、动态可选「第几条」),SC / 上舰预览把接收方渲染成真实的该 UP;真实拉取失败时自动回退示例数据 (0108e43, 0e967f3, ea7f22a, eeb63a0)
- 动态卡版式细化:话题标签并入正文块顶部、附加内容(预约等)独立成块、转发的内部原动态跟随同一套版式渲染 (8e9230f, 2ac9ee8, 2391330)

### Fixed

- 动态图集重复 @全体:开启「推送动态图集」时,一条图文动态的主卡片与图集附图会各 @ 一次全体成员;现图集附图不再重复 @,仅主卡片 @ (49ebe14)
- 卡片渲染冷启动竞态:服务器刚启动、puppeteer 浏览器尚未热身时,多张卡片并发渲染会把同一张卡平铺成 2×2 并裁切;现已将渲染串行化,彻底消除竞态 (e3e8b52)
- @全体 不再阻塞卡片与后续推送:无管理权限的群发 @全体 被协议端拒绝并重试时,不再拖住卡片正文与推送队列,@全体 改为即发不等待、失败仅异步记入历史 (5783940)
- 超大 B站 CDN 图片在内联前先压缩,避免渲染超时 / 内存膨胀 (b09ef4c)
- Desktop 桌面端:外链改用系统浏览器打开,右键菜单在 Tauri 外壳内正常工作 (35a0959, 61e875d)
- 关于页赞助者名单文案订正:由「每日自动同步」改为「每次发布新版本时同步」,与实际 CI 机制一致 (fb2132f)

---

## [0.1.0-alpha.13] — 2026-06-23

弹幕词云停用词入口 + 断流接续(下播延迟判定)。版本号从 `alpha.11` 直接跳到 `alpha.13`:原拟毕业的 `0.1.0` 正式版暂缓,其内容降级为下方 `alpha.12`(不单独打 tag,改动随本次 `alpha.13` 镜像一并发布)。

### Added

- 弹幕词云停用词配置入口:高级规则「直播总结」分类下新增停用词设置(英文逗号分隔,追加到内置中文停用词表后再分词),全局与 per-UP 覆盖均可配置 (12a6788)
- 断流接续:直播阈值新增「断流接续」开关 + 等待时长(1–10 分钟,默认 2)。开启后 UP 下播先延迟判定,等待窗口内重新开播即接续为同一场直播(弹幕 / 时长 / 词云沿用首次开播基线),用于吸收网络抖动 / 超管掐流导致的瞬时断流误报;全局与 per-UP 均可配置 (879994d)

---

## [0.1.0-alpha.12] — 2026-06-22

汇总 `0.1.0-alpha.11` 以来的改动。原拟作为 `0.1.0` 正式版首发(从 alpha 毕业),现暂缓毕业、继续 alpha 迭代。

### Added

- 「关于」页:赞助者名单(随独立端发版构建时从爱发电同步生成静态文件)、更新日志、项目信息三个分区 (01b443e, e8a8116, 3737336)
- 响应式分区导航 SectionNav:顶部 tab 支持左右箭头滚动并隐藏原生滚动条,窄屏不再折叠 (ea0c407, e620414)

### Changed

- 概览「今日」统计改为按**本地时区** 0 点计算 —— 凌晨时段的推送不再被 UTC 日界算到昨天,并新增「今日失败」数 (f31efe6)
- QQ 官方机器人:订阅里「@全体」开关在 QQ 官方目标上禁用并提示「不支持」(后端本就 best-effort 跳过该平台的 @全体,避免用户误以为会生效) (5631863)
- 移除未使用的 web-dashboard 推送平台:存量配置中的该类适配器 / 目标在加载时自动清理,不影响其它推送目标 (b91cc7b)

### Fixed

- iPad / 窄屏顶部 tab 折叠:Targets / Rules / Logs 改用 SectionNav,并把 sticky tab 锚定到实测表头高度消除滚动漂移;Rules「添加 UP」下拉不再被顶栏遮挡 (d900bc3, 756d6fc, 5ed4f31)
- 更新日志 tab 切换时的高度抖动 (a7823c4)
- 日志运行区随页面滚动,与更新日志一致 (3d5ffa7)
- 推送历史失败行对齐:失败标记改为内嵌,行不再错位 (1557988)

---

## [0.1.0-alpha.11] — 2026-06-19

### Added

- 新增 QQ 官方机器人(q.qq.com)推送平台:Targets 页配置适配器(appId / appSecret / 沙箱开关 / 公私域 botType),按会话 scope 寻址(频道 guildId+channelId / 群 groupOpenid / C2C userOpenid),内置会话发现选择器(读已捞 openid 列表自动发现 + 手动枚举子频道);私域机器人图集走 markdown 图文合并,媒体内容与卡片图合并下发 (db2a48c, c352baa, d60290f)
- Dashboard 暗色模式:支持明 / 暗主题切换,全站配色语义 token 化,卡片描边 / 头像环 / Targets / Cards 等内联硬编码色同步适配暗色 (3f6a398, 4cb1e49, 1383fc3)

### Fixed

- dev / 12-factor(legacy)模式下自动探测 Chrome 并一键热启用卡片渲染后,`chromePath` 现也写回 cwd 的 `bn.config.{yaml,yml,json}`,下次启动直接复用、无需重新探测(纯 env / cli 无配置文件时仍不写回)(f5e0777)
- 历史记录列表对 @全体提醒、弹幕词云两类无正文推送不再显示「(无内容)」:@全体段落写「@全体」、词云写「[弹幕词云]」,与图集 `[图集 N 张]` 同一思路(仅作用于新写入记录)(ac619f4)

---

## [0.1.0-alpha.10] — 2026-06-15

### Added

- 卡片渲染未配置 Chrome 时，预览区新增「自动探测 Chrome」按钮：探测本机常见安装位置，命中后一键热启用卡片渲染（运行时构造 puppeteer 注入 live / dynamic 引擎）并写回 `bn.config.yaml`，无需重启 (5f56201)

### Changed

- 高级规则的 per-UP 覆盖编辑接入草稿灵动岛：编辑某个 UP 的覆盖项时，未保存改动汇总到底部灵动岛统一保存 / 丢弃，与全局规则一致，移除页内独立的保存 / 丢弃按钮 (8cc9bfb)

### Fixed

- 窄视口（iPad / 缩小窗口）下日志页布局坍缩：将渐入动画的残留 transform 与 sticky 侧栏解耦，避免侧栏在单列布局下覆盖日志内容 (1f03a21)

---

## [0.1.0-alpha.9] — 2026-06-11

### Fixed

- 加密 / 付费 / 测试等受限直播间不再无限重连刷屏：建立弹幕 WS 前先预检弹幕连接信息，B 站明确拒绝（明确的非风控错误码 / 无 token / 无弹幕服务器列表）时判定为受限房并停止该房间监测、仅告警一次；普通房间与临时网络失败的重连 / watchdog 行为保持不变 (9af0e14, 77f26f2)
- 直播弹幕连接预检补上 wbi 签名：B 站 `getDanmuInfo` 现已强制 wbi 签名，此前未签名的预检请求固定被风控拦成 `-352`、一路回退空转，现改为附加 `wts` + `w_rid` 签名后预检真正生效、受限房识别归位、`-352` 告警噪音消除 (6c56938)
- `-352` 等风控 / 校验拦截只视为预检不确定并回退到直接建连，避免误杀普通房间 (77f26f2)
- Dashboard 删除推送目标时同步清理订阅 routing / @全体引用，避免残留指向已删除目标的无效配置 (77f26f2)

---

## [0.1.0-alpha.8] — 2026-06-06

### Added

- Webhook 推送支持钉钉 / 飞书 / 企业微信机器人 provider；Generic 模式保持旧 JSON envelope,平台机器人按文本消息协议发送,非文本通知沿用文字摘要降级 (bd8effc)

### Changed

- Webhook adapter 保存 URL 后自动维护默认投递目标,订阅页可直接选择,无需手动新建 PushTarget (bd8effc)
- Webhook adapter「测试」改为真实发送测试推送,并回写 adapter / target 最近测试状态 (bd8effc)

### Fixed

- 禁止外部手动创建 / 修改 / 删除 Webhook 托管目标；删除 Webhook adapter 时同步清理订阅 routing / @全体引用,避免残留无效 target (bd8effc)
- Webhook 平台业务响应解析与错误脱敏补强:解析钉钉 / 飞书 / 企业微信业务码,错误返回和日志不泄漏 webhook URL、key、secret、sign、token (bd8effc)

---

## [0.1.0-alpha.7] — 2026-06-01

### Added

- Docker 镜像同步发布到 GHCR:`ghcr.io/akokk0/bilibili-notify`,tag 与 Docker Hub 保持一致

### Fixed

- 兼容旧版 / 手写 `bn.config.yaml` 缺少 `webDistDir` 的 Docker 配置:配置未写时先回退 `BN_WEB_DIST`,再检查镜像默认 `/app/web-dist/index.html`,避免控制台 `GET /` 返回 404
- Docker compose / config 示例补齐 `webDistDir` 说明,部署文档强调只挂载目录并让容器首次启动自动生成配置
- Desktop 发布链路补强:恢复 macOS 图标、保护 loopback dashboard、退出时清理 Node sidecar、Windows 打开动作避开 shell、精简 runtime staging、固定内置 Node 版本并检查产物内容

### Build

- Docker builder 不再执行 `changeset version`,避免独立端镜像构建在 changeset 后重装依赖阶段卡住

---

## [0.1.0-alpha.6] — 2026-05-30

### Added

- 日志页新增「运行日志 / 更新日志」二级导航,更新日志按需加载 `apps/CHANGELOG.md` 渲染 (0cf1e32)

### Changed

- 概览页各模块卡片不再显示模块版本号,仅保留系统状态里的核心 / 面板版本 (0cf1e32)

### Fixed

- 直播 WS 连接静默半开后自动自愈:超过 180 秒无心跳 / 消息活动会重连,连接关闭也统一进入重连流程,避免长时间漏开播推送 (b81039f)
- 动态接口返回字符串或缺失 `pub_ts` 时不再整条跳过:数字字符串按发布时间处理,毫秒时间戳归一化为秒,缺失时尝试解析 `pub_time` 兜底 (0ed9d98)

### Build

- Docker 构建上下文保留 `apps/CHANGELOG.md`,确保更新日志页在镜像内可正常打包

---

## [0.1.0-alpha.5] — 2026-05-28

### Added

- 动态推送文案模板可自定义:Rules 页「动态消息模板」全局编辑 + 高级规则 per-UP 覆盖,
  普通动态 / 视频投稿两段独立,变量 `{name}`(UP 名)/ `{url}`(链接,关闭附带 URL 时为空)
  (5eb594a)

### Changed

- 直播消息模板去掉启用开关,与动态模板一致 —— 改了即生效;默认值等于内建文案,
  未编辑时推送输出不变 (c88bb52)
- 直播 / 上舰 / 特别关注 / 弹幕总结模板占位符统一为 `{name}` 风格,变量提示同步修正
  (删掉渲染器不提供的 `{title}`、`{duration}`→`{time}`、补 `{follower}`/`{follower_change}`、
  上舰改 `{uname}`/`{mname}`/`{guard}`);老 `-name` 写法仍兼容 (5eb594a)

### Fixed

- 动态推送「有图 / 无图」两条分支文案不一致:无图分支重复前缀(`X发布了一条动态：X发布了
  一条动态：…`),现统一走同一模板渲染 (5eb594a)
- per-UP 模板 override 被全局默认值污染:只改了某一项模板(如直播总结)的 UP 会被误判为
  自定义了动态模板,从而停止跟随全局动态模板、面板误标「已定制」(0c50955)
- 改全局动态模板后未热重载,无 per-UP 覆盖的订阅仍用旧模板;新增订阅的 per-UP
  覆盖(过滤 / 图集 / 模板)首次推送丢失,需等下次刷新 (17f0412)

---

## [0.1.0-alpha.4] — 2026-05-27

### Added

- 灵动岛草稿机制:Rules / Cards / Ai / System 四页修改字段时,屏幕底部漂浮 chip
  统一显示「页名 + 未保存字段数 + 保存」按钮,hover / click 展开后按 section 分组
  的字段级 diff list(旧值 → 新值),点单行跳转高亮目标 Field,左下「丢弃全部更改」
  一键回滚。chip 5 态:idle 不显示、dirty(待保存)、saving(保存中 spinner)、
  saved(✓ 1.2s 自动消失)、error(摇晃 + 红边 pulse 需手动 dismiss)。dirty 态
  chip 外圈 2px 粉色单弧流光环绕岛旋转 2.4s/圈。AiBar dismissed / expanded 时灵
  动岛垂直避让(4fcd59f..af13f66)

### Fixed

- 独立端 dashboard 修改全局 ai persona / cardStyle 后 dynamic 推送不跟随 hot-reload,
  仍用 add 订阅时的旧值(直播端因 refreshOps 不受影响);`buildDynamicSubsView` /
  `buildLiveSubViewSingle` 改为仅在 `sub.overrides.cardStyle` / `.ai` / `.filters`
  真存在时生成对应字段,推送路径回退到 `imageRenderer.config` / `commentary.config`
  全局兜底 (5bdedcc)
- 保存 AI 配置时,即便只改 persona / 温度 / 提示词也会触发连通性探活,导致按钮卡
  10s;后端 `shouldRunAiEnableCheck` 加值对比,patch 含连接字段但值跟 current 相同
  时不触发 (0395404)

---

## [0.1.0-alpha.3] — 2026-05-26

### Added

- @全体提醒拆为独立消息(@全体 → 卡片 → 文字三条),Koishi 主插件、advanced-subscription
  per-UP 覆盖、独立端 globals.@全体规则同步暴露 (f427278)
- 动态过滤新增「图文」「视频」两个屏蔽开关 (f427278)

### Fixed

- SC 卡片右侧文字贴边塌陷 (f427278)
- 上舰卡片长用户名挤掉舰长 / 提督 / 总督 logo (f427278)
- 直播卡片简介里 `<p>` / `<br>` 等富文本残片改为去标签纯文本(独立端镜像 image
  渲染路径同步生效)

### Build

- image-release workflow 5 个 inline shell step 提到脚本文件 (28b1e4e)
- 修复 codex audit 抓出的 7 类 CI 安全 / 一致性问题(包括 RELEASE_PAT 注入路径 /
  tag commit 鉴权 / merge job 串行化等)(22dce62)
- pnpm 11.1.3 → 11.3.0,docker / setup actions 升到最新版 (22854f5)
- arm64 镜像 build cache mode=max → mode=min,绕开 GHA cache export hang (7da55c6)
- tag push 鉴权改 basic auth,兼容 fine-grained PAT(`github_pat_*` 上不支持 bearer)
  (81a274e)

---

## [0.1.0-alpha.2] — 2026-05-24

### Added

- MasterNotifier 同步消费 `auth-lost` 与 `engine-error`,主人通知文案对齐独立端 (e600703)
- image-release workflow 自动创建 GitHub Release 挂在 `v<version>` tag 下 (87a1232)

### Fixed

- onebot 合并转发 node 用 bot 真身的 uin / nickname,而非订阅 master 假名 (f47810e)
- onebot forward send 先校验 target,latency 计入 `get_login_info` (db8ef70)

### Build

- image-release workflow build 拆 matrix(amd64 / arm64 各原生 runner)+ digest
  merge 替代 emulation,大幅缩短 arm64 镜像构建时间 (5cfbb89)
- docker actions 升到 v4/v7,checkout 升到 v6 (5ed8382)
- builder 阶段插 `vp --version` / `vp env doctor` 诊断输出 (74901a4)
- 显式 `pnpm install --no-frozen-lockfile` + `pnpm -r build` 拆两阶段 + `--stream`
  便于定位 hang (2a4d110, c41de2a)
- tag push + release 创建走 `secrets.RELEASE_PAT`(workflow `GITHUB_TOKEN` 没有
  workflow scope 写权)(8311a67)
- docker pull 命令省 `docker.io/` 前缀 (49a2805)

---

## [0.1.0-alpha.1] — 2026-05-24

### Added

- 独立端 cards 配置补齐 `font` / `hideDesc` / `hideFollower` 三字段(对齐 koishi
  image 子插件 schema)(7022322)
- 动态图集开关独立成 `imageGroup` 子段,支持 per-UP 覆盖 (ca47fe6)

### Changed

- 二维码渲染收进 `LoginFlow` 默认实现,koishi 端 / 独立端不再各自实现一遍 PNG 输出
  (7d32e87)
- koishi 端 config 模型整体收敛,internal 当唯一默认源;packages/internal 同步
  export `DEFAULT_AI` / `DEFAULT_CARD_STYLE` / `DEFAULT_TEMPLATES` 等(独立端通过
  共用 packages/internal 间接受益)(e0083e2)
- 直播卡片 `room_info.description` 富文本(`<p>` / `<br>` / entity-encoded)统一剥
  成 plain text(`html-to-plain.ts`)(77bbc77)
- forward-images sink 走 `payload.forward` 二分:默认普通群消息多 image segment
  (稳),显式合并转发节点(`h("message", { forward: true }, nodes)`)(5c632f7)
- 动态图集推送改用合并转发消息形态 (c3ee457)

### Fixed

- dashboard 未启用鉴权时误弹 LoginDialog 且走不出去 (22cb87e)
- onebot 错误响应加 NapCat 掉线提示文案 (dfb4388)
- onebot 私聊 scope target 报「group: groupId missing」(77b9b37)

### Breaking Changes

- `image` 子插件 `followerDisplay` 字段重命名为 `hideFollower` 并反转语义
  (`followerDisplay: true` → `hideFollower: false`)(106b3db, b9aaba6)
- `GlobalDefaults.imageGroup` 新子段:
  - 新增 `imageGroup: { enable, forward }`(老 `globals.json` 缺字段时按默认值兜底)
  - `Subscription.overrides.dynamic` 重命名为 `Subscription.overrides.imageGroup`
    (旧数据需外部迁移或 dashboard 重写一次)
  - `AppConfig` 删除原顶层两字段
  - `forward-images` payload 加 `forward: boolean` 区分合并转发卡片 vs 普通多图
  - 详见 ca47fe6

### Build

- 镜像 push 成功后自动打 git tag `v<version>` (dfdcb6f)
- `.dockerignore` 排除 `**/*.md` 误杀 changeset 文件 (1be8d4c)

---

## [0.1.0-alpha.0] — 2026-05-22

### Added

- monorepo 拆分后独立端首次镜像。业务核心独立成平台中立的 `@bilibili-notify/*`
  包,独立端通过 `apps/server` 消费这套核心,经 puppeteer + Hono 提供 HTTP API +
  WebSocket,`apps/web` (React 19 + Vite) 内嵌进同一镜像
- 独立端版本由 `apps/server/package.json#version` 驱动(本次起为 `0.1.0-alpha.0`),
  bump 后 push 到 dev 触发 image-release workflow 自动构建 + push Docker Hub +
  打 git tag + 创建 GitHub Release (9b7bb75)
