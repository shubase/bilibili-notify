lib
dist
external

node_modules
npm-debug.log
tsconfig.tsbuildinfo

.eslintcache
.DS_Store
.idea
.vscode
!.vscode/
.vscode/*
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln

.interface
.bn-design
.bugs

# 本地审计/评审产物,不入库
docs/review/

# README 界面截图 —— 托管在 GitHub Release 的 `assets` tag 上,README 引绝对链接。
# 本地留一份是为了好重传(`gh release upload assets docs/images/dashboard/*.png --clobber`),
# 不入库是为了别让反复换的大图沉进 git 历史。
docs/images/dashboard/

# 群公告 txt 本地草稿,不入库(锚定根,避免误伤 .claude/skills/release-notes/)
/release-notes/

# vp config 生成的 hook 脚手架(git hooks 实际由 lefthook 装进默认 .git/hooks)
.vite-hooks/

# Tauri 桌面端本地构建/资源暂存
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/
apps/desktop/src-tauri/resources/
apps/desktop/.tauri/

/data/
/.local-data/

# Claude Code 的 worktree 暂存目录(EnterWorktree 会建在仓库内)
**/.claude/worktrees/

# Claude Code 的本机私有设置(权限放行等),不入库
.claude/settings.local.json
